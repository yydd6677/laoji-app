# These documentation-only values intentionally do not point to the LaoJi
# deployment. Keep APP_ENV=development here: an actual production submission
# rejects reserved example domains, while production-rehearsal is only for a
# signed HTTPS/WSS build-pipeline rehearsal.
APP_ENV=development
EXPO_PUBLIC_LAOJI_API_BASE=https://api.example.com
EXPO_PUBLIC_MEETING_API_BASE=https://api.example.com
EXPO_PUBLIC_REALTIME_ASR_HOST=api.example.com
EXPO_PUBLIC_REALTIME_ASR_PORT=443
EXPO_PUBLIC_REALTIME_ASR_SECURE=true
EXPO_PUBLIC_REALTIME_ASR_PROVIDER=qwen
EXPO_PUBLIC_PRIVACY_POLICY_URL=https://www.example.com/privacy
EXPO_PUBLIC_TERMS_OF_SERVICE_URL=https://www.example.com/terms
EXPO_PUBLIC_ACCOUNT_DELETION_URL=https://www.example.com/account-deletion
# Release A writes a shadow SQLite copy while existing screens keep reading legacy storage.
EXPO_PUBLIC_LOCAL_MEETING_DB_V1=true
# Release B read enrollment is explicit and fail-closed; keep disabled until
# the per-scope shadow import and dual-read preflight are both consistent.
EXPO_PUBLIC_LOCAL_MEETING_DB_CANONICAL_READ_V1=false
# Canonical writes remain opt-in and require canonical reads.
EXPO_PUBLIC_LOCAL_MEETING_DB_CANONICAL_WRITE_V1=false
# Account root and upload transport stay off until the deployed API is verified end to end.
EXPO_PUBLIC_LOCAL_MEETING_DB_ACCOUNT_ROOT_WRITE_V1=false
EXPO_PUBLIC_LOCAL_MEETING_DB_ACCOUNT_UPLOAD_WRITE_V1=false
