# 候选回滚目录恢复记录（2026-08-19）

## 事件

在更新隔离 `18021` 的实时协议诊断时，一条错误的候选部署命令误删了远端
`/home/zhong/laoji-vnext-candidate/releases` 目录。生产 `18020/8030`、生产数据库、公网入口、GPU1、PCB、
Smart Meeting 和其他服务没有被访问或修改，但原候选目录的文件系统状态短暂受影响。

## 恢复

- 现有候选 tar 包恢复了 `34c488d`、`6a80576`、`71cf466`、`7f70ca4`、`db8259a`、`f6974e0` 和 `b631452` 的源/部署目录。
- 对所有有部署记录且本地 Git 可回溯的版本，从对应提交重新生成并恢复了：
  `ccb65f0`、`47640f3`、`81fe81e`、`9f7cbbd`、`958c9f2`、`b4f84c9`、`bb38e64`、
  `bd93690`、`c72472c`，以及重复确认的旧版本。
- 当前候选服务运行在 `releases/candidate-current/services/laoji-api`，使用独立候选数据库；恢复过程没有覆盖当前候选数据。

## 不可伪造的部分

- 被误删目录中未包含在 tar 包或 Git 源快照中的旧运行日志、临时文件和旧候选 SQLite 副本无法恢复；它们不属于生产数据，不能用重新生成的空文件冒充原始证据。
- Stage 5 删除门仍保持关闭；本记录不构成删除授权，也不把重建的源快照宣称为原始运行状态。
