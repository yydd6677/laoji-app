# vNext 隔离候选构建 1.1.12 (120)

状态：`candidate-only`。这是与当前实现分支重新对齐的隔离 APK，不是生产发布包；不安装到其他任务设备，也不切换服务端 capability barrier。

## 构建身份

- applicationId：`com.laoji.app`
- versionName：`1.1.12`
- android.versionCode：`120`
- APK：`artifacts/vnext-candidates/1.1.12-120/laoji-vnext-candidate-1.1.12-120.apk`
- 文件大小：`82,535,483` bytes
- SHA-256：`936c827128aad0f5d02a994769672cde625796ea7aa7e162e8426bd69f9d2df0`
- 签名：APK Signature Scheme v2，单签名者；当前项目 debug keystore，仅用于隔离候选
- 服务端候选包：`artifacts/vnext-candidates/1.1.12-120/laoji-vnext-server-candidate-c1eee30.tar.gz`
- 服务端包大小：`836,675` bytes
- 服务端包 SHA-256：`1dbf69d179ec5f94b7f706545819eee17655ccc36bf953e7dc42a45f61162da6`
- 最新服务端候选包：`artifacts/vnext-candidates/1.1.12-120/laoji-vnext-server-candidate-471d94f.tar.gz`
- 最新服务端包大小：`838,265` bytes
- 最新服务端包 SHA-256：`648786f4b0f5b585c44dc00cca6c3b5ac20125a89b83e90e75b6cce0e214e633`
- 当前服务端候选包：`artifacts/vnext-candidates/1.1.12-120/laoji-vnext-server-candidate-de5e514.tar.gz`
- 当前服务端包大小：`837,132` bytes
- 当前服务端包 SHA-256：`b59698dc66721122e04c46f40fde01d11af50f1e34bdb08e03743ba41805e139`
- 最新服务端候选包：`artifacts/vnext-candidates/1.1.12-120/laoji-vnext-server-candidate-0eae538.tar.gz`
- 最新服务端包大小：`838,044` bytes
- 最新服务端包 SHA-256：`b7ecae7e6147570e90c51ca0ca922554b7b282599a9973d454601ea0a51f4c9c`
- APK Git 源码基线：`6f7bd51`；最新服务端包 Git 源码基线：`0eae538`；Android 原生工程由 Expo prebuild 生成并被仓库忽略，
  `app.config.js` 是版本来源。构建时另外核对了生成的 Gradle metadata，避免生成工程残留旧版本。

## 编译开关

使用 `APP_ENV=development`、`EXPO_PUBLIC_API_BASE=https://laoji.cloud` 构建，并将下列候选开关编译为 `true`：

- `scheduleGraphV2Candidate`
- `realtimeAsrV2Candidate`
- `meetingQuestionsQ2Candidate`
- `meetingSummarySourceStreamCandidate`
- `nativeProjectionEnvelopeCandidate`

Expo 配置输出、Gradle `output-metadata.json`、`aapt dump badging` 和 APK 签名元数据均一致。

## 构建验证

- `./gradlew :app:assembleRelease --no-daemon`：成功，`524 actionable tasks`。
- 重新生成/刷新 Android 工程后，必须再次执行同一 metadata 核对；不能只依据 `app.config.js` 报告版本。
- 服务端包只包含 `services/laoji-api`、`services/laoji-asr`、`deploy/linux`、`contracts/vnext` 和
  `tools/vnext`；已排除凭据、数据库、音频、缓存和 Python 字节码。候选包仅上传到隔离服务器，
  不代表生产部署。
- 最新服务端包基线为 `0eae538`，在 `de5e514` 的基础上修复静音 PCM 被 ASR 幻觉为短词：只有
  RMS/峰值均低于部署门限时才不进入模型，并返回稳定 `no_speech`。`de5e514` 修复长会议强制证据选择：关键纠正、否定、
  负责人和明确时间表达保留，普通数字/连接词只在预算内稳定填充；三份完整长会议真实 embedding
  证据包回放通过，但尚未进行完整 Facts 质量验收或 capability 切换。`c1eee30`、`9c63cd3`、
  `49b1acf`、`471d94f`、`de5e514` 旧包仍保留用于回滚。`0eae538` 包已上传到隔离服务器，其中
  ASR 和 API 分别运行在 loopback `8031/18021`，并以 `LOCAL_DEVICE=cpu` 和显式 CAM++ 权重路径
  ready；18020/8030 和公网流量未改变。
- `aapt dump badging`：`com.laoji.app`, `versionCode=120`, `versionName=1.1.12`。
- `apksigner verify --verbose`：v2 签名通过。
- TypeScript、Python 编译和 Stage 0/1/2/3/4 合同验证见阶段证据文档。

## 限制

- 只证明候选客户端可构建并携带候选配置；不证明服务端已接受候选流量。
- Stage 2 仍缺专属 Android 设备的断网/杀进程回放、公开旧提交周期和 capability barrier。
- Stage 3/4 的真实语义、设备和页面验收仍未闭合；Stage 5 删除审计仍为 `safe_to_delete=false`。
- 当前 APK 使用项目 debug keystore，不能作为生产发布签名包。
