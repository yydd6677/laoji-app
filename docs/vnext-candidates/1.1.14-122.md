# vNext 隔离候选构建 1.1.14 (122)

状态：`candidate-only`。这是 vNext 实施分支的隔离候选 APK 与服务端包，不是生产发布包；未安装到真机，未切换服务端 capability barrier，未发布公网。

## 构建身份

- 源码分支：`vnext/implementation`
- 服务端源码提交：`a736833`（Stage 4 退出预检）
- applicationId：`com.laoji.app`
- versionName：`1.1.14`
- android.versionCode：`122`
- APK：`artifacts/vnext-candidates/1.1.14-122/laoji-vnext-candidate-1.1.14-122.apk`
- APK 大小：`82,545,751` bytes
- APK SHA-256：`267f2c2aeb98c902c14f7faae4c9cde3d90bcad8dd93ceabac2d20be92ed3555`
- 服务端候选包：`artifacts/vnext-candidates/1.1.14-122/laoji-vnext-server-candidate-a736833.tar.gz`
- 服务端包大小：`836,447` bytes
- 服务端包 SHA-256：`c531f1ffacbf4d532aefcd83aed2351eebca73cbf409a27af932cf3bd5f1db56`
- 签名：APK Signature Scheme v2，单签名者；当前项目 debug keystore，仅用于隔离候选

## 编译开关

候选 APK 中以下能力开关均为 `true`：

- `scheduleGraphV2Candidate`
- `realtimeAsrV2Candidate`
- `meetingQuestionsQ2Candidate`
- `meetingSummarySourceStreamCandidate`
- `nativeProjectionEnvelopeCandidate`

这些开关只证明客户端可以携带新合同。服务端能力仍由隔离环境和持久 barrier 控制，稳定入口、生产流量和公网配置保持不变。

## 构建验证

- `npx expo prebuild --platform android --no-install`：通过。
- `./gradlew :app:assembleRelease --no-daemon`：通过，Gradle `BUILD SUCCESSFUL`。
- `aapt2 dump badging`：确认 `com.laoji.app`、`versionCode=122`、`versionName=1.1.14`。
- `apksigner verify --verbose`：V2 签名通过；V1/V3/V4 未启用，符合当前隔离候选签名方式。
- Expo 配置回读：版本与五个候选开关均符合预期。
- 构建期间 Metro 尝试写入相对 `TMPDIR` 缓存时出现一次 `ENOENT` 缓存告警，但 JS bundle、APK 和 Gradle 构建均成功；下一次构建应使用绝对临时目录，避免该告警。

## 当前明确未完成项

- 未安装到真机或专属 LaoJi Android 设备；没有断网、杀进程、网络切换、页面重建和连续转写的设备证据。
- Stage 2 realtime 候选单次约 `16.224s`，尚未达到蓝图的端到端 p95 `<=2s`。
- 缺少独立人工日程 holdout、会议事实/问答人工质量和引用相关性审阅。
- Stage 4 的真实手机页面重建、FTS、人工 holdout 和旧 schedule 零流量周期尚未闭合。
- Stage 5 删除审计仍为 `safe_to_delete=false`：五个 capability barrier 尚未激活，旧链路仍有约 `291` 个引用，公开零流量周期、旧任务排空和候选数据库完整性证据缺失。
- 隔离候选服务仍运行在 `18021/8031`；生产 `18020/8030/21434`、GPU1、PCB、Smart Meeting 和公网入口未修改。

## 回溯与使用边界

该目录中的 APK 与服务端包可用于后续隔离设备/隔离端口验收。它们不应直接安装到用户设备、替换生产 APK、上传更新清单或切换 capability barrier。若后续验收失败，稳定回滚基线仍为 `1.1.10 (118)`，而本候选的源码回溯点为 `a736833`。
