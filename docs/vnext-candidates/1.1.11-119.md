# vNext 隔离候选构建 1.1.11 (119)

状态：`candidate-only`。这不是生产发布包，不安装到当前受其他任务使用的模拟器，也不切换服务端 capability barrier。

## 构建身份

- applicationId：`com.laoji.app`
- versionName：`1.1.11`
- android.versionCode：`119`
- APK：`artifacts/vnext-candidates/1.1.11-119/laoji-vnext-candidate-1.1.11-119.apk`
- 文件大小：`82,535,483` bytes
- SHA-256：`b79e8a08cd0602eeddd95f895e41172c1026798e4cb51241f056afdef3bb8119`
- 签名：APK Signature Scheme v2，单签名者；当前项目 debug keystore，仅用于隔离候选

## 编译开关

本次构建使用 `APP_ENV=development`、`EXPO_PUBLIC_API_BASE=https://laoji.cloud`，并将下列候选开关编译为 `true`：

- `scheduleGraphV2Candidate`
- `realtimeAsrV2Candidate`
- `meetingQuestionsQ2Candidate`
- `meetingSummarySourceStreamCandidate`
- `nativeProjectionEnvelopeCandidate`

这些开关仍是客户端候选开关，不代表服务端已经接受对应流量；生产默认和回滚基线不变。

## 已执行验证

以下检查在源码工作树 `/home/yydd/LaoJi-worktrees/vnext-implementation` 执行并通过：

```text
npm exec -- tsc --noEmit --pretty false
python3 -m compileall -q services/laoji-api/app services/laoji-asr tools/vnext
python3 tools/vnext/verify_stage0_exit.py
python3 tools/vnext/verify_stage1_exit.py
python3 tools/vnext/verify_stage1_migrations.py
python3 tools/vnext/verify_stage2_android_contract.py
python3 tools/vnext/verify_stage2_migrations.py
python3 tools/vnext/verify_deployment_templates.py
python3 tools/vnext/verify_stage3_source_stream_contract.py
python3 tools/vnext/verify_stage4_migration.py
python3 tools/vnext/verify_projection_checkpoint.py
```

APK 元数据和签名还通过了 `aapt dump badging`、`apkanalyzer` 和 `apksigner verify --verbose` 核对。

## 未完成与限制

- Stage 2 仍未退出：专属设备的 WorkManager 断网/杀进程实机证据、GPU ASR/讲话人真实质量、旧提交公开周期和 capability barrier 尚未闭合。
- Stage 3 的 Facts V3/Q2 仍为隔离候选，人工事实支持率、引用相关性、行动质量和长会议覆盖尚未达到退出门。
- Stage 4 的日程自然语料金标、真实页面重建和完整设备验收尚未完成。
- Stage 5 删除审计为 `safe_to_delete=false`；旧读写引用和 capability barrier 仍存在，不能删除旧链路或模型。

因此本构建只证明“候选源码可以编译并携带候选配置”，不能证明生产服务已切换、所有服务稳定或可以替代 `1.1.10 (118)` 稳定版本。
