# 设备原始音频临时留存验收（2026-08-09）

使用临时设备上传一段真实会议视频提取的 6 秒 WAV，并等待设备转写任务进入 `completed` 后，直接检查生产数据库资产记录和服务器文件系统：

- 资产任务状态为 `completed`，资产仍绑定本机 epoch。
- `upload_state=processed`。
- `storage_path` 已清空，原始音频文件不存在。
- 转写结果可以通过设备接口读取；只保留生成的转写/后续整理数据，不保留设备原始音频。
- 测试会议、epoch、设备主记录和临时文件均已清理；清理使用 `PRAGMA foreign_keys=ON`，修复后 ready 和完整性检查仍正常。

该证据直接验证“服务器临时接收原始数据、生成数据可留存”的去账号化隐私边界。
