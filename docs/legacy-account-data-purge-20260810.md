# 去账号化后的旧账号数据清理（2026-08-10）

## 范围与前提

当前产品已改为本机主数据、设备 epoch 服务；用户已确认没有真实用户且不要求历史数据完整性。因此本次只清理 `data_epoch_id IS NULL` 的旧账号会议和账号存储，保留当前设备 epoch 的 7 条会议、230 条转写和设备身份墓碑，不触碰 ASR、Ollama、GPU1 或其他服务。

## 清理结果

- 旧账号会议：98 条 → 0；旧账号转写：4,310 条 → 0。
- 旧账号 RecordingAsset：76 条 → 0；所有服务端源音频路径清空，`audio_files` 下 95 个文件已删除。
- 旧账号整理任务：37 条 → 0；保留的唯一设备整理任务不含原始请求正文。
- 旧账号 FinalSummary：168 条 → 0；当前设备生成结果未删除。
- 账号数据库：17 个用户、111 个会话、33 条远程日程及命令记录 → 0。日程现在只由手机本机数据库提供。
- 旧账号声纹：8 个 profile、embedding 和识别日志 → 0；设备声纹表未留下 `data_epoch_id IS NULL` 的 profile。

## 安全与恢复边界

- 操作前通过 SQLite backup API 创建了临时快照；`local.db`、`schedule.db`、声纹库快照哈希分别为：
  - `12f462f316db87a677b463cd0c26217a91b65c7c5aa74259487a912316b079ee`
  - `183dcf07173df99b2e046c01fd45afa6c5467cded54111460f47da7234e46f48`
  - `fa7f994d3400058da0ca84244a573a57e6ee2ca2e0af8c0f117c1b82ebfcde73`
- 快照仅用于本次事务验证，确认公网 ready、队列和三库完整性恢复后已删除，不作为长期原始数据副本。
- 清理后主库、日程库和声纹库均 `PRAGMA integrity_check=ok`、外键违规为 0、WAL 正常；`laoji-api` 已重新启动，公网 `/api/ready` 返回 `ready=true`，队列为 0。

## 保留边界

当前设备 epoch 的会议、转写和匿名设备身份仍保留，以便设备重新接入后继续使用；源音频已按设备策略不留在服务器。问答质量、真人声纹纵向质量和真机验收仍是独立延期项。
