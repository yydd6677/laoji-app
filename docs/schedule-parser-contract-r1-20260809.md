# 日程解析合同修复与运行证据（2026-08-09）

本记录只覆盖本轮实际修改和验证，不替代原始架构计划。

## 这轮修复

- 有明确日期但没有钟点时，规则和模型归一化都输出 `is_all_day=true`；没有日期的草稿仍保持空日期、不可保存，不凭空填今天。
- 单一开始钟点（模型省略结束时间或重复开始/结束时间）统一补默认一小时结束时间；相对时间提醒也遵守该规则。
- 日期范围的无年份起点固定在请求参考年份，只有终点月份早于起点时才跨到下一年；`7月21号到7月18号` 保留 `2026-07-21` 为起点、清空终点并要求确认顺序。
- 模型没有从原文听到提醒时，丢弃模型臆造的提醒分钟；明确说出的提醒和相对时间“之后提醒”仍保留。
- 清理模型标题中的“刚才/刚刚”等口语控制壳，避免更正句把时间上下文写进标题。

## 当前源码与回滚

- 活动解析器 SHA-256：`6452ebdcf77a9f8372d0b9ba9bbaa6c7b9ff9dc4e2631070bfaef63d6f702b5c`。
- 活动测试 SHA-256：`944e4ce5b6d2062c3abfbd298efb86761429c951b72bc2a5c5a393d35fb7190b`。
- 修改前备份：服务器 `compact-production/backups/20260809-schedule-contract-r1/`，解析器备份哈希 `a3b32150400994349cb5946894699836017a1737212bd0b17910dc8d110489c3`；测试及地址/ASR旧断言备份同目录。

## 测试结果

- 日程解析质量文件：`65 passed`。
- 精简生产针对性集合（设备合同、拓扑、安全、地址、统一日程 ASR、LLM、检索、存储，加上日程质量）：`91 passed`。
- 生产 venv 未安装 pytest；pytest/pytest-asyncio 仅放在 `/tmp/laoji-test-deps`，SQLite、音频和 speaker 临时目录均隔离到 `/tmp`。
- 完整历史套件在旧断言尚未全部迁移时的现场结果为 `292 passed / 37 failed`；失败主要集中于硬编码旧模板 revision、会议问答旧调用次数/来源收敛预期、旧摘要引用结构，以及少量迁移夹具。问答专题按用户决定延期；这些失败没有被改测试伪装成产品验收通过。

## 重启后的真实 HTTP 样本

`laoji-api.service` 已重启，`/api/ready` 返回 `ready=true`，ASR/9B/embedding/VAD/CAM++ 就绪，队列和活动租约为 0；服务仍为 `active/enabled`。

- `schedule-all-day-r1`：`明天开会` → `2026-07-14`、`is_all_day=true`、无需追问。
- `schedule-reverse-r1`：`7月21号到7月18号提交材料` → 起点 `2026-07-21`、终点为空、中文顺序澄清、`route=quick`、未调用模型。
- `schedule-relative-r1`：`十五分钟后提醒我提交材料`（参考时间 23:50:48）→ `00:05-01:05`、提醒 `0` 分钟、无需追问。
- 之前的 `schedule-contract-r1`：`明天下午三点开会` → `15:00-16:00`、`route=quick`。

## 未宣称完成的范围

真机验收、会议问答专题、真实声纹纵向质量、完整历史旧断言清理仍按当前计划保留为未完成；本轮只收口日程解析的真实缺口和精简生产相关合同，不把窄测试结果扩大成全目标完成。
