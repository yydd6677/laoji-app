# LaoJi vNext Stage 0 facts

## Scope

This is a read-only fact freeze for the vNext implementation worktree. It does not
authorize production cutover, service restart, APK installation, public traffic
changes, or deletion of any current data.

## Mobile baseline

- Published and local baseline: `1.1.10`, Android `versionCode=118`.
- Source HEAD observed before the isolated worktree: `48e3b36` (`docs: record real-device install smoke`).
- APK: `artifacts/vnext-stage0/release/laoji-1.1.10-118.apk`.
- APK size: `82,278,571` bytes.
- APK SHA-256: `b5ce1ce8e3f5dc42043d0ed2e4cf90094f43ee887322701328cec4cbc67fc04f`.
- `tools/app-update/latest.json` SHA-256: `66303c3a838f426dd710ce111bcade9ed30612dfd120c84cf3bfe0529b2726a7`.
- The manifest and APK agree on version, size, URL and SHA-256.

The source snapshot is intentionally dirty: the original worktree had 539 tracked
diff paths and 153 untracked paths. The isolated worktree `vnext/implementation`
contains that state as a baseline projection; no original worktree changes were
reverted or cleaned.

## Live server facts (read-only)

- Host: `zhong-server` (`183.36.243.124`).
- `laoji-api.service`: active, `127.0.0.1:18020`.
- `laoji-asr.service`: active, `127.0.0.1:8030`.
- `laoji-ollama.service`: active, `127.0.0.1:21434`.
- Production API cwd: `$SERVER_DEPLOYMENT_ROOT/compact-production/backend`.
- The production backend cwd is not a Git repository and has no usable Git HEAD.
- Source inventory command covered `app`, `qwen_asr_service`, `tests`, `alembic` and
  `scripts`: 768 files, 14,896,038 bytes, aggregate SHA-256
  `6c67a7ae674fa7bc23c65b5473ea2aab8dbcbda007e719af1f65ab4e54a85174`.
- The isolated source import is split into `services/laoji-api` and `services/laoji-asr`.
  `.env*`, databases/WAL/SHM, audio, summaries, logs, models, backups, pycache and
  pytest/ruff caches were excluded.

## Production database snapshots

SQLite Online Backup API was used on the server before copying. Each local snapshot
passes `PRAGMA integrity_check` and is ignored by Git under `artifacts/vnext-stage0/db/`.

| database | bytes | SHA-256 | integrity | tables |
| --- | ---: | --- | --- | ---: |
| `local.db` | 7,155,712 | `db01fb0c443199f37a8ace7caae2c99d16074cb54cd8483ce62189ea1fada26` | `ok` | 56 |
| `schedule.db` | 221,184 | `681c3ebe2b000c15bbf17d2317122d474deaaed2a750bac3de571c697127e293` | `ok` | 10 |
| `speaker_voiceprints.db` | 1,310,720 | `4a0749364fd55c4b56e92666208802d6db4d38014ba3112e5424e622837686b5` | `ok` | 3 |

The snapshots are migration input only. They are not copied into application source,
not committed, not uploaded, and not used as a second runtime database.

## Stage 0 remaining gate

The facts, APK, source import and snapshot evidence exist. Stage 0 is not yet exited:
the imported backend must still pass an isolated build/import check, the generated
vNext schemas and current-to-target inventory must be created, and a local baseline
commit/tag must be made without including database snapshots or credentials.
