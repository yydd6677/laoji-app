# LaoJi vNext Stage 0 exit

## Result

Stage 0 passed on 2026-08-18. This record freezes the reconstructable 1.1.10 (118)
baseline and authorizes isolated Stage 1+ implementation only. It does not prove
production adoption or authorize deployment, service restart, public traffic change,
APK publication, device installation, data deletion, or GPU reallocation.

## Frozen baseline

- Annotated tag: `vnext-stage0-1.1.10-118`.
- Commit: `cdd44ec04394e17c0bba1c946eb38df3c5720690`.
- APK: `1.1.10`, Android `versionCode=118`, 82,278,571 bytes.
- APK SHA-256: `b5ce1ce8e3f5dc42043d0ed2e4cf90094f43ee887322701328cec4cbc67fc04f`.
- The release manifest agrees with the APK version, size, URL and SHA-256.

## Exit evidence

- `local.db`, `schedule.db` and `speaker_voiceprints.db` were copied with the SQLite
  Online Backup API and pass `PRAGMA integrity_check`.
- The database snapshots and APK are ignored local recovery artifacts and are not
  committed.
- The production source import is versioned under `services/laoji-api` and
  `services/laoji-asr`; runtime data, credentials, models, logs and caches were
  excluded.
- The API and ASR source snapshots pass isolated import checks from the frozen tag.
- Generated contracts pass from the frozen tag (`13` artifacts) and the current
  branch (`22` artifacts).
- `CURRENT-TO-TARGET-20260818.json` inventories 69 database tables, 184 FastAPI route
  decorators, the legacy summary worker and the two imported services.
- `tools/vnext/sqlite_snapshot.py` provides a portable SQLite backup/integrity tool.
- `tools/vnext/verify_stage0_exit.py --check-inventory` fails closed on baseline,
  contract, import or inventory drift.
- No credential file was included in the source import or Stage 0 evidence commit.

## Recovery boundary

Recovery means checking out the frozen tag, verifying the tagged contract artifacts,
and restoring a database snapshot only into an isolated recovery location before an
integrity check. The snapshots are migration inputs, not a second runtime database.
Production rollback still requires an explicit deployment decision and is outside
this exit record.

## Production boundary

Stage 0 performed read-only production inspection and SQLite online snapshots. It did
not restart or reconfigure production services, change Cloudflare/public routing,
touch GPU1, PCB or Smart Meeting, publish/install an APK, or mutate live databases.

Re-run:

```text
.venv-vnext/bin/python tools/vnext/verify_stage0_exit.py --check-inventory
```
