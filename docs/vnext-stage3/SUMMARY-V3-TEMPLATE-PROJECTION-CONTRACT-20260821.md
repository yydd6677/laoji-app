# Stage 3 Facts V3 四模板本地投影合同

状态：`deterministic projection and Android candidate replay passed; Stage 3 not adopted`。

## 修正

`MeetingTemplate.sectionSchema` 仍描述旧模板结构，而 Facts V3 已使用另一组稳定块键。当前
`1.1.45 (153)` 的模板选择页因此显示“关键讨论”“支持与改进”等旧说明，和实际生成的“主要议题 / 关键
结论”“支持需求”等结构不一致。候选将四个模板的可见说明和稳定块键对齐到
`projectMeetingFactsV3`，未改变弹层层级、样式、触摸范围或模板 ID：

- 通用：概述、主要议题、关键结论；
- 1:1：概述、讨论主题、反馈与关注、支持需求；
- 项目同步：概述、进展、风险与阻塞、范围和里程碑；
- 访谈：概述、主题、受访者观点、证据摘录、后续问题。

行动候选仍只在既有行动区域显示一次，不伪装为模板 section；代表性引用、数字、依赖流程和方案对比
继续由事实内容确定性增补。

## 可执行合同

`tools/vnext/test_summary_v3_projection.mjs` 使用同一份包含主题、进展、结论、风险、问题、引用、时间线、
行动和备选方案的 Facts V3 文档验证：

- 四模板共享同一 `documentId/sourceFingerprint`，投影不修改事实文档；
- 行动正文、负责人、期限、适配度、证据分和引用在四模板间完全一致；
- 所有模板声明的核心稳定块均真实存在，且没有独立“决定”模块；
- 转写引用只锚定当前 Transcript segment；
- 编辑覆盖只作用于当前模板块，不修改共享事实或其他模板；
- Markdown 中行动候选只出现一份；
- 暖态连续 100 次本地切换实测约 `7ms`，低于单次 `100ms` 预算，不产生网络调用。

与既有 activation-fence 合同合并运行共 `26/26` 通过，`npx tsc --noEmit` 通过。

## Android 候选回放

候选 APK `1.1.46 (154)` 已构建并覆盖安装到老记专用 `emulator-5562`：

- APK 大小约 `79 MiB`，SHA-256 为
  `077a4ca15981467f52efab2dea506f3c935fb2697c60f926c491c7a7265d87a5`；
- Release 冷启动 `TotalTime=1095ms`，重启恢复 `TotalTime=1027ms`，未出现白屏或应用崩溃；
- 模板弹层实际显示修正后的四组结构说明，四个模板逐项切换均成功；
- 切换期间隔离候选 API 日志行数保持 `139 -> 139`，未发生模板网络请求；
- 选择“项目同步”后强制停止并重启 App，仍恢复“项目同步”投影；
- 整理版本页的当前项始终为原有 `8月20日 21:34 · 通用整理`，没有因模板切换创建新版本；
- 当前投影继续显示同一概述、逐字引用和行动来源，模板只改变本地组织方式。

本次只更正文字合同和稳定块元数据，没有修改颜色、间距、触摸区域或主题组件；因此只对标准蓝做了
比例化可见回放，没有把重复的绚彩主题截图当成新的功能门。

## 边界

本证据关闭纯本地模板投影、旧说明漂移以及新 APK 上的四模板切换、版本历史和进程重建窗口；不替代
独立人工事实/行动质量、富块全类型两主题视觉回放、公开零旧链路周期或 capability barrier。生产
`18020/8030`、公网、GPU1、PCB 和其他服务均未改变。
