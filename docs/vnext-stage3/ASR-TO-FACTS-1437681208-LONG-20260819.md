# 长会议完整视频 ASR 到 Facts V3 回放

状态：`isolated candidate evidence; not adopted`。

## 执行边界

- 使用 `/home/yydd/下载/会议视频样本/1437681208-1-192.mp4` 的完整 78 分钟音频，先经隔离
  `8031 /v2/asr/batch` 转写，再在隔离 Ollama 上生成 Facts V3。
- 生产 `8030/18020`、公网入口、GPU1、PCB、Smart Meeting 和其他用户服务未修改。
- 转写正文只在回放进程内存中流转；报告只保存媒体/转写哈希、段数、覆盖率、模型调用次数和耗时。
- 字幕未参与本次输入，仅作为外部弱参考，不能把本结果解释为人工事实支持率。

## 结果

| 指标 | 结果 |
| --- | ---: |
| 音频时长 | `4,690,027 ms` |
| ASR 分片 | `336` 个，14 秒/片 |
| ASR 文本段 | `335` |
| ASR 候选耗时 | `1,193,963 ms` |
| Facts 证据总段 | `168` |
| 纳入证据段 | `59` |
| 输入估算 | `10,126 tokens` |
| embedding | `true`，真实 `qwen3-embedding:0.6b` |
| Facts 模型调用 | `1` |
| Facts / action | `1 / 0` |
| Facts 生成耗时 | `18,511 ms` |
| 总耗时 | `1,228,588 ms` |
| 候选合同结果 | `passed=true` |

机器可读报告：`asr-to-facts-v3-1437681208-long-20260819.json`。

## 解释和未闭合门

本回放证明完整长视频可以走隔离 ASR、超预算 embedding/MMR 和一次 Facts 生成，未发生长度拒绝、
协议截断或第二个模型 owner。`source_coverage=0.35119` 是预算内证据选择比例，不是事实支持率；
`facts=1` 也不能证明会议内容质量足够发布。

仍缺独立人工事实/行动审查、source-stream 设备端上传恢复、Android 页面恢复和 capability barrier，
因此不能切换生产或删除旧链路。
