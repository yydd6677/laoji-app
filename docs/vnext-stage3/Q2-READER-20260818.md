# Stage 3 Q2 reader candidate

- status: `isolated candidate; not production`
- observed: 2026-08-18 Asia/Shanghai
- worktree: `/home/yydd/LaoJi-worktrees/vnext-implementation`
- production mutation: `none`

## 已闭合的实现边界

1. 服务端新增 `POST /api/device/v2/meetings/{binding_id}/questions-v2`。请求使用
   Pydantic `extra=forbid` 合同，包含 schema/provider revision、binding generation/revision、
   cancel revision 和不可变来源列表；接口默认由 `LAOJI_VNEXT_Q2_READER_ENABLED` 关闭。
2. reader 每次只调用一次 `LlmProvider`，输入只包含当前转写、用户本次勾选的“我的笔记”和来源哈希；
   不读取整理结果、旧答案或历史问答上下文。`answer` 必须由连续分句完整覆盖，所有引用必须落在
   当前来源的 UTF-8 字符边界并逐字匹配。
3. 移动端 Q2 仓储拥有 snapshot/thread/turn/clause/citation；Q2 结果不会写入旧的
   `meeting_question_*` 表。页面重进从 Q2 仓储只读投影，引用仍定位到当前文字记录或我的笔记。
4. Q2 任务使用本机 `device_operations` 作为可恢复的运行标识。失败重试更换 operation 并原子
   重新绑定 pending turn，成功后只提交一次；来源 fingerprint 变化会要求新问答线程。
5. 现有问答 UI 只有在 `meetingQuestionsQ2Candidate` 与远端 `question_reader_v2` 同时开启时
   选择 Q2；默认 APK、稳定问答和服务器生产流量不变。

## 验证

- `tests/test_vnext_question_reader.py` 与 `tests/test_device_v2_api.py`：`12 passed`，覆盖 reader
  单次调用、来源 hash、binding fence、额外字段和 capability gate。
- Stage 3 source-stream/summary worker/Q2 聚焦回归：`23 passed`。
- 扩展 `tests/test_vnext_*.py`、Facts V3、章节合并、持久任务回归：`122 passed`。
- `npx tsc --noEmit`：通过。
- `python3 -m compileall -q services/laoji-api/app services/laoji-api/tests`：通过。
- `git diff --check`：通过。

## 尚未关闭的门

- 真实 Qwen/Ollama 语义 holdout：有据回答、`not_stated/cannot_confirm` 拒答、引用相关性和延迟
  尚未形成可发布证据；当前测试使用 fake provider，不代表模型质量。
- LaoJi 专属模拟器/真机上的问答 UI、网络中断、进程死亡、重进恢复和设备 capability 回放；
  当前连接的 `emulator-5560` 属于其他任务，不能作为证据。
- Q2 与 Facts V3 的统一 capability barrier、legacy drain、资源/privacy 审计和生产切换尚未完成。
