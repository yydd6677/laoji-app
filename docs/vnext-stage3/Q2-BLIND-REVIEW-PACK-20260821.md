# Stage 3 Q2 独立盲审包与安全投影回放

状态：`isolated candidate automated replay passed; human review pending; Stage 3 not adopted`。

## 边界

- 候选 API：经本机 `28030` 连接服务器 loopback `127.0.0.1:18030`。
- Provider：本地 `qwen3.5:9b`，Q2 `question.reader.v2`，每题一次 reader 调用。
- 来源：`/home/yydd/下载/会议视频样本` 中当前 10 份完整 SRT；字幕只作为弱参考，不进入生产
  prompt、规则或样本专用补丁。
- 生产 `18020/8030`、GPU1、PCB、Smart Meeting 和公网流量没有修改。

## 本轮暴露并关闭的候选缺口

首次 27 题回放有 4 题被严格 grounding 拒绝。保留原失败证据后，候选只做以下确定性投影：

1. 模型在有依据分句旁生成无依据分句时，删除该分句；全部分句均无依据时仍 fail-closed。
2. `1.`、`2.`、`3.` 等列表序号不再被当作业务数字；小数和正文中的数量仍须逐字引用。
3. “未提及”分句的空引用不再使已验证的兄弟分句失败；空引用分句本身不对外显示。
4. provider 引用逐字跨越相邻 ASR 行时，只在同一来源/revision、确实跨边界且严格文字/极性
   校验通过后，拆回两条当前来源引用。虚构 quote 仍被拒绝。

完整来源同时陈述“希望每人 400 左右”“不会严格卡到 400”与“三四百可能就有权益”。因此
`survey-threshold` 的完整来源弱参考从只接受“三四百/300”修订为也接受逐字支撑的 `400`；这是
评测元数据修正，不是生产回答规则。

## 自动化结果

- 完整来源问题：`27/27` 通过。
- 显示引用逐字匹配：`100%`。
- 暖态、模型支持的 Q2：p50 `3.296s`，p95 `11.529s`，最大 `25.269s`。
- Q2 单元测试：`57/57`；盲审包/恢复测试：`5/5`。
- 哈希报告：`/dev/shm/q2-human-review-20260821-r2-report.json`，SHA-256
  `3cec73e65d18e991cca264e91dbde657862f58c1e756dec65f2b6ef02a0ce38b`。

报告只保存 case ID、输入/问题/回答哈希、计数、耗时和错误码，不保存问题、回答、引用、正文或凭据。

## 私有盲审包

- 路径：`/home/yydd/.cache/laoji-vnext/q2-human-review-20260821-r2.json`（Git 工作树外）。
- 权限：`0600`。
- 行数：`27`，随机顺序；不包含样本 ID、自动通过状态或期望答案。
- SHA-256：`081136b7d241abac30af98de0a2e561af899e2a21a18f1c32ca1f0fbee3dc0d1`。
- 每行待审字段：回答正确性、完整性、引用相关性、拒答适当性和严重错误。
- 评估器会在每题后原子写入哈希检查点和私有包；`--resume` 不重复已完成题，
  `--resume --retry-failed` 只替换失败题。

## 未关闭项

该文件没有伪装成人工验收。盲审字段仍为空，因此 Q2 的独立人工正确性/引用相关性 `>=95%` 尚未
关闭；Facts/行动候选的独立事实支持、重要遗漏和行动适用性盲审结果也尚未完成。Stage 3 的公开零旧链路
周期与 capability barrier 同样未通过，不能采用 Q2、切生产或删除 Q0。

该文件现在明确作为冻结来源包，不再把其中单个 `reviewer` 字段当成完整验收合同。两名评审、第三人
裁决、哈希绑定和确定性评分使用
[Stage 3 独立人工质量证据合同](HUMAN-QUALITY-EVIDENCE-CONTRACT-20260821.md)；私有模板已经生成，但
没有伪填任何人工判断。
