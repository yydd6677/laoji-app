# Stage 3 真实候选回放复核（2026-08-19）

状态：`isolated candidate evidence; not adopted`。

## 执行边界

- 通过既有只读 SSH 隧道调用服务器隔离 Ollama；未重启或修改服务器进程、数据库、R2、生产
  18020/8030、公网入口、GPU1、PCB 或其他服务。
- 输入来自 `/home/yydd/下载/会议视频样本` 当前快照的同名 SRT 窗口。字幕只作弱参考，可能错误或
  遗漏；没有进入生产 prompt、few-shot、规则、源代码或服务端持久数据。
- 报告只保存 case ID、来源哈希、计数、阶段和延迟，不保存转写正文、模型回答或引用正文。

## 结果

| 能力 | 用例 | 结果 | 备注 |
| --- | ---: | ---: | --- |
| Facts V3 | 9 | `9/9` | 结构、来源覆盖、弱主题、重复行动和宏观目标门通过 |
| Q2 reader | 27 | `27/27` | 回答、未提及、部分回答、冲突拒答和引用结构门通过 |

报告文件：

- `facts-v3-real-holdout-rerun-20260819.json`
- `q2-real-holdout-rerun-20260819.json`

Facts V3 的 9 条用例均使用 1 次模型调用，除 `negotiation` 触发一次允许的结构修复外没有第二次
调用；该用例的总延迟约 44.03 秒。其余用例约 6.04--14.50 秒。Q2 暖态单题约 0.88--3.99 秒，
冲突输入在本地确定性门禁下约 1--2 毫秒。

## 不能据此关闭的门

- 这些是字幕窗口，不是完整视频解码、转写或长会议证据包回放。
- 尚无独立人工事实支持率、行动有效性和引用相关性盲审；`9/9`、`27/27` 不是人工质量通过率。
- 尚无 LaoJi 专属 Android 设备页面、断网、token refresh、杀进程和连续 stable/final 回放。
- capability barrier、旧任务排空、资源/privacy 门、生产切换和 Stage 5 删除审计仍关闭。

