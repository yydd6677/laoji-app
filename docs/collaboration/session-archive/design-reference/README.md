# Design Reference Images

This folder archives the original visual reference screenshots from:

```text
/home/yydd/LaoJi/design/
```

They correspond to the ambiguous Claude-session attachment path:

```text
/home/yydd/LaoJi/design/粘贴的文件.png
```

The exact attachment filename was not present on disk. The four WeChat image files in the source directory were copied here because their SHA-256 hashes match the images already embedded in the Figma Make export at:

```text
../figma-make/Generate React components/src/imports/
```

The duplicate storage is intentional: `design-reference/` preserves the original source names, while `figma-make/` preserves the generated UI export structure.
