# 当前工作目标：飞书日历重构与发布质量

更新日期：2026-07-15

## 目标

把老记 Android 移动端完善为可发布候选版。日历结构、通用组件和动效以飞书 Android 7.71.8 源码为基线，删除老记无法提供或明确不需要的协作能力；语音创建、会议录制、转写和总结保持老记自己的产品路径。

详细产品例外与审阅规则见 `docs/feishu-calendar-baseline-contract.md`，语音入口见 `docs/voice-schedule-interaction-contract.md`。本文件是现阶段实施顺序和完成条件的权威清单。

## 当前状态

- 本文件从最终审阅账本抽取的可实施项已全部落地，并有源码、单元/集成测试、Android release 产物及两轮模拟器 smoke 证据。
- 仍有三项发布外部证据不能由当前工作区自行补齐：飞书原生/Rust 布局响应 golden fixture、Android 实体机 TalkBack 定向验收、正式 HTTPS/WSS 域名与最终法务地址/发布签名。
- 未取得 golden fixture 前只声明当前重叠布局满足本项目的确定性合同，不声明与飞书不可见算法 1:1；模拟器 UIAutomator 无障碍审计也不冒充实体机 TalkBack。
- 正式 production 构建保持 fail-closed：缺少最终域名或出现 HTTP、裸 IP、占位地址时主动拒绝构建；当前 APK/AAB 是内部 release 候选，不是商店提交包。

## 已建立的基线

- 只保留月视图和单日视图，视图按钮直接切换，一周从周日开始。
- 月视图已具备连续跨日条、固定槽位、相邻月预取和源码对齐的基本几何。
- 日视图已具备 25 个小时标签、48 个半小时创建区、全天区域和跨午夜创建。
- 搜索、详情、编辑、年月选择和底部导航已迁入新的飞书化页面结构。
- 日程加号短按打开创建方式面板，长按可沿四分之一圆弧拖选语音或手动创建。
- 语音页支持短按持续录音和按住松开结束，并保护异步解析退出状态。

## P0：先保证数据正确和版本可交付

1. 为重复事件建立稳定引用 `{ sourceEventId, occurrenceDate }`，统一搜索、详情、编辑、删除和提醒。
2. 将冲突检测下沉到事件仓库，按目标日期范围查询并展开相关重复实例，不依赖当前屏幕缓存。
3. 将删除、撤销、提醒取消和失败回滚改成确定状态机；延迟 DELETE、失败 DELETE 和并发撤销后，服务端及本机都必须恰好保留预期的一条事件。
4. 增加通知响应导航和可恢复注册表；前台、后台、冷启动点击提醒都进入正确日程详情，不产生重复或残留提醒。
5. 所有运行源码、测试和公开合同必须进入版本控制，并从干净检出重跑完整门禁。

## P1：统一事件语义与恢复能力

1. 提取语音和手动创建共用的事件草稿校验器，覆盖非法日期、非法时分、单边时间、反向区间、跨午夜和重复规则。
2. 为缓存增加版本化 schema、损坏隔离和恢复提示；月份缓存改为有界缓存，加载与错误按范围隔离。
3. App 回到前台时去重刷新当前月、相邻月和搜索目录；离线继续显示缓存并保留可恢复错误。
4. 统一 `00:00` 结束边界，使月视图、日视图、搜索和日期筛选使用同一半开区间语义。
5. 修复按重复实例实际日期搜索、远期实例详情和编辑。
6. 时间编辑器的可见值、无障碍值和保存值保持一致，并覆盖短屏与横屏。

## P1：完成飞书日视图交互

1. 复杂重叠布局改为每个事件独立矩形、完整显示宽度和稳定绘制/命中顺序；无法从反编译源码恢复的布局算法使用真实 fixture 验证。
2. 支持单日定时事件的长按移动、上下边缘缩放、时间吸附、边缘自动滚动和保存失败回滚。
3. 左右换日、标题选日和异步刷新保持纵向位置、全天展开状态和内部滚动位置。
4. 收敛无障碍树，只暴露可见日期、事件和必要创建入口；使用 Android TalkBack 真机验收。

## P2：组件与动效收口

- 年月面板增加全屏透明触摸拦截，阻止点击穿透并支持点外收起。
- 详情头部增加速度/半高吸附和嵌套滚动消费。
- 搜索增加关键词高亮和稳定的按压覆盖反馈。
- 统一确认框、动作面板、底部覆盖层的圆角、遮罩和完整入退场。
- 清理固定坐标、旧文案和旧页面假设的 Android 验收脚本。

## 已确认裁剪

- 不实现多日历、日历账号、三日/列表视图和六入口底栏。
- 不复刻参与人、会议室、群聊、RSVP、签到和飞书会议联动；老记会议模块独立存在。
- 第一版事件统一主色，不做分类颜色。
- 登录账号当前不承诺离线写入队列；断网时必须明确失败或只读缓存，不能伪装已同步。

## 已确定产品决策

1. **语音保存路径**：保留语音确认页直接保存的快速路径，同时与手动创建共享同一校验器和事件仓库；“编辑更多内容”才进入完整编辑页，不为了代码复用强迫每次语音创建多跳一页。
2. **全天冲突语义**：全天事项与定时事项只显示软提示，不阻止保存；全天与全天、定时与定时按明确重叠规则提示。
3. **重复事件编辑范围**：实现“仅此日程、此后日程、全部日程”三种范围，再开放重复实例的拖动和删除。
4. **时区范围**：v1 按设备时区显示，数据模型先保留事件时区字段；独立时区选择器延后，不保留只有外观没有能力的入口。
5. **实施拍板机制**：审阅中仍需选择但不改变既定产品边界的技术与交互细节，默认采用审阅建议并直接落实到合同、测试和代码；只有新增能力、扩大服务端权限、改变已确认裁剪或发布边界时，才重新请求产品确认。

## 审阅账本完整覆盖

本节是最终审阅账本所有待完成项的可追踪投影。上方章节负责排序，本节负责防止低优先级或边界问题在归并时丢失。重复结论映射到同一任务。

### 交付与数据正确性

- [x] 重复实例稳定引用贯穿搜索、详情、编辑、删除和提醒。
- [x] 冲突查询按目标范围加载并展开重复实例；落实已确定的全天软提示语义。
- [x] 删除、撤销、通知取消和失败回滚形成确定事务。
- [x] 通知前台、后台、冷启动回到正确详情，注册表故障可恢复。
- [x] 运行源码、测试和公开目标进入 Git；发布前仍需从干净克隆重跑门禁。
- [x] 飞书源码基线取代旧 Figma/Expo Go 会话说明，个性化能力另有合同。

### 状态、缓存与事件语义

- [x] 语音文字/澄清/录音解析具有运行代次，关闭后旧结果不回写；保存期间不可关闭。
- [x] 手动编辑保存退出竞态得到同等保护。
- [x] 语音与手动创建使用共享草稿校验器及事件仓库写入。
- [x] 缓存具有版本化 schema、损坏隔离和恢复提示。
- [x] 月份缓存有界，加载与错误按月份范围隔离。
- [x] 回到前台去重刷新当前月、相邻月和搜索目录。
- [x] 同月份并发刷新使用请求序号，旧响应不能覆盖新响应。
- [x] 时间编辑器日期范围、分钟显示、无障碍值和保存值一致。
- [x] `00:00` 结束在月、日、搜索和日期筛选中统一为半开区间。
- [x] 重复事件可按实际 occurrence 日期搜索并打开远期实例。
- [x] 全天归类边界和全天区排序使用统一合同。
- [x] 提醒合同完整：定时重复实例使用 90 天滚动窗口；全天/无具体时间日程按 v1 决策不猜测触发时刻。

### 日视图与重复事件

- [x] 复杂重叠布局支持每项独立矩形、可扩宽尾段和稳定绘制/命中顺序。
- [ ] 外部证据：取得无法从 Java 反编译恢复的布局 golden fixture 后，才声明与飞书不可见算法等价。
- [x] 单日定时事件支持长按移动、上下缩放、吸附、边缘滚动和失败回滚。
- [x] 换日、标题选日和异步刷新保持正文纵向位置。
- [x] 全天展开及内部滚动位置跨换日保持并按内容夹取。
- [x] 实现重复事件“仅此、此后、全部”作用域和截止日期/自定义星期所需模型。
- [x] 重复实例移动、删除和冲突检测使用已选择的作用域。
- [x] 日视图事件点击防重复导航，滚动或布局变化清理临时草稿。
- [x] 无障碍树只暴露可见日期、事件和必要创建入口；模拟器 UI 树审计通过。
- [ ] 外部证据：Android 实体机 TalkBack 定向验收。
- [x] 12/24 小时显示遵循系统设置；v1 不提供独立时区选择入口。

### 页面、组件与动效

- [x] 视图模式持久化，离开或重启后保留月/日选择。
- [x] 年月滚轮补慢拖 `onScrollEndDrag` 收尾。
- [x] 月视图和快速日期面板在跨午夜及回前台时刷新“今天”。
- [x] 年月面板使用全屏透明命中层，点外关闭且阻止穿透。
- [x] 月份程序化切换、展开行收起和换日使用已审阅的分段动画。
- [x] 详情头部具有速度/半高吸附和嵌套滚动消费。
- [x] 详情及编辑在宽屏采用受约束容器，紧凑屏保持全屏。
- [x] 时间编辑页在 320x568 和 640x360 下全部控件可达。
- [x] 搜索关键词高亮，按压使用稳定尺寸的中性色覆盖。
- [x] 搜索空态使用正式视觉资产，不以通用图标代替。
- [x] 通用确认框和动作面板统一圆角、遮罩、分隔线及关闭行为。
- [x] 语音底部层补完整退场和遮罩命中；无拖拽行为时不显示拖拽把手。
- [x] FAB 阴影、位移、旋转和长按圆弧反馈统一验收。
- [x] 底部导航迁到 navigator 级单一所有权，页面不重复渲染。
- [x] Android smoke 移除剩余固定坐标、旧页面文案和旧布局假设。
- [x] 覆盖层补系统返回和焦点隔离，隐藏页面不得抢占返回或无障碍焦点。

### 架构、测试与交付

- [x] 事件仓库提供稳定的实例引用、范围查询、冲突查询和事务命令，页面不直接拼装缓存语义。
- [x] 按所有权提取身份、缓存、删除/编辑事务、重复作用域和通知模块；不做无行为收益的机械拆分。
- [x] 建立 CI，执行 TypeScript、Jest、Python、Expo Doctor、公开边界、凭据扫描、生产依赖审计和 Android release 配置门禁。
- [x] 为事件身份、事务、提醒、缓存和日历布局设置关键覆盖率门槛及故障注入测试。
- [x] Android smoke 使用本次构建的 APK、语义节点和动态日期；截图只留证，不冒充像素断言。
- [x] 持久化回归首轮清空、后续保留状态，覆盖升级重装、重启、缓存迁移和月份缓存增长。
- [x] 从独立干净 Git 候选目录验证全部源码、公开合同、补丁和构建脚本可复现。

### 明确不实施

- [x] 三日、列表、多日历、日历账号和六入口底栏。
- [x] 参与人、会议室、群聊、RSVP、签到和飞书会议联动。
- [x] 日历/参与人/会议室搜索筛选；个人日程的日期筛选仍可实施。
- [x] 第一版分类颜色和登录用户离线写入队列。

## 实施顺序

1. 重复实例身份、范围冲突、删除撤销、通知回跳。
2. 共享草稿校验、缓存 schema、前台刷新和午夜边界。
3. 日视图复杂布局、移动缩放和滚动保持。
4. 快速年月、详情吸附、搜索反馈和公共弹层动效。
5. Android 真机回归、生产 HTTPS 配置、AAB 和干净克隆发布门禁。

## 完成门禁

- [x] `npx tsc --noEmit --incremental false` 通过。
- [x] Jest 95 个套件、787 项测试通过，覆盖竞态、故障注入、复杂重叠和生命周期。
- [x] Python 48 项辅助测试、Expo Doctor 18/18、公开边界与 production 依赖审计通过。
- [x] Android 内部 release APK/AAB 从独立干净候选目录构建，不依赖 Metro 或 Expo Go。
- [x] 模拟器冷启动与升级重装两轮 smoke 通过，分别审计 30/22 份 UI 树；持久化游客会话恢复通过。
- [ ] 外部证据：实体机验证通知回跳、两种录音方式、月/日切换、跨午夜、重复实例和 TalkBack。
- [ ] 外部输入：提供正式 HTTPS/WSS 域名、最终法务地址及商店发布签名后生成 production 提交包；门禁已验证会拒绝 HTTP、裸 IP、占位地址和不合规签名。
