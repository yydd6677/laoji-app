# Stage 2 导入转写性能回放

状态：`isolated CPU evidence; CPU route rejected`。

本报告保留被否决的 CPU/8 秒片段证据。选定 GPU0 架构在复用已校验压缩媒体后已通过首段/RTF 门，
见 [校验媒体复用与 GPU0 回放](VERIFIED-MEDIA-CACHE-GPU-20260820.md)；Stage 2 的其他采用门仍开放。

## 实现切片

- 导入转写继续使用首批最多 4 秒、后续最多 32 秒的批处理策略。
- ASR 片段上限保持 4 秒；尝试将后续片段放宽到 8 秒后，真实回放的 CPU RTF 变差，已回退默认值。
- 每批记录只写队列、推理、墙钟和批次大小，不写正文、对象 key、文件名或人物信息。
- 客户端已验证：首批稳定文字在 final 之前进入文字记录，终态只产生一个 final；v47 稳定段唯一索引和重放去重生效。

## 真实回放

使用隔离 `18022`/`8031` 和当前会议样本派生的 30 个真实 M4A，顺序执行，R2 上传、VAD、8031 ASR、稳定事件、final 事件和 cleanup obligation 均走真实链路。

| 指标 | 结果 | 门限 | 结论 |
| --- | ---: | ---: | --- |
| 文本成功 | 30/30 | 30/30 | 通过 |
| 单 final | 30/30 | 30/30 | 通过 |
| 首段 p50 | 5.780 s | - | 观测 |
| 首段 p95 | 11.486 s | <= 8 s | 未通过 |
| 导入 RTF p50 | 0.553 | - | 观测 |
| 导入 RTF p95 | 0.704 | <= 0.5 | 未通过 |
| cleanup | 31 条处理 | 0 pending | 通过 |

机器报告：`stage2-import-perf30-segment8s-edbf225.json`（服务器候选工作区）。

## 根因边界

回放在候选 CPU ASR 上执行；生产 `8030`、`18020`、GPU0/GPU1、PCB 和其他服务未切换。8 秒片段实验降低了段数但增加了单次推理负载，未改善 p95，因此没有保留该回归。该轮证明 CPU 路线的阻断是 ASR 资源/性能门，不是媒体格式、R2、稳定事件、NO_SPEECH 或数据一致性失败。

本 CPU 证据不授权 capability barrier、生产流量切换或旧链路删除，也不再作为选定架构的性能结论。

后续线程拓扑复测见 `CPU-THREAD-TOPOLOGY-20260819.md`：当前跨双路 56 线程的 4 秒暖态调用为
`3.211-4.761s`，单 NUMA/28 线程反而退化，未采用；CPU 完整导入性能门未通过。
