# vNext 后端回归复核（2026-08-19）

本记录只更新测试事实，不授权生产切换、设备安装或 capability barrier。

## 环境与结果

项目专用环境：`.venv-vnext`，`ENV=local`，`PYTHONPATH=services/laoji-api`。

```text
ENV=local DATABASE_URL='sqlite+aiosqlite:////dev/shm/laoji-vnext-full-20260819-r2.sqlite' \
SECRET_KEY='test-secret-key-012345678901234567890123' PYTHONPATH=services/laoji-api \
  .venv-vnext/bin/python -m pytest -q services/laoji-api/tests --disable-warnings

508 passed, 10 failed, 246 warnings in 78.73s
```

候选聚焦集合（Stage 2/3/4 合同、设备 v2、R2、实时事件、来源流、Facts V3、Q2、投影和任务恢复）
单独复核：

```text
ENV=local DATABASE_URL='sqlite+aiosqlite:///:memory:' \
SECRET_KEY='test-secret-key-012345678901234567890123' PYTHONPATH=services/laoji-api \
  .venv-vnext/bin/python -m pytest -q \
  services/laoji-api/tests/test_device_v2_api.py \
  services/laoji-api/tests/test_device_v2_realtime_api.py \
  services/laoji-api/tests/test_recording_assets_v2.py \
  services/laoji-api/tests/test_summary_v3.py \
  services/laoji-api/tests/test_summary_v3_chapter_merge.py \
  services/laoji-api/tests/test_vnext_*.py --disable-warnings

146 passed, 17 warnings in 3.60s
```

## 失败边界

10 个失败均位于稳定版旧 owner 或时间敏感旧合同：

- `app_meeting_question` 旧实现仍包含多轮路由/复核和旧来源压缩断言；Q2 reader 候选不调用该 owner。
- 旧模板 revision=1 与当前候选 revision=2 不一致；模板 v3 目标仍未跨 barrier。
- 旧 compact summary 两轮模型调用断言与候选的一次事实生成方向冲突。
- 账号密码重置迁移测试写入 2026-07-01/02 的待处理记录；按当前日期和 30 天保留策略已正确过期，
  该测试样本时间未更新。
- 旧访谈模板 fallback 断言要求把概述复制到受访者观点，和当前不重复投影规则冲突。

这些失败没有被降级、词法兜底或恢复旧模型调用来“修绿”。在对应 capability barrier 关闭旧写入、
完成 drain 后，应更新或删除相应旧测试合同；在此之前稳定 v1 兼容路径仍保留，不能物理删除。

## 仍未闭合

- LaoJi 专属 Android 设备的断网、token refresh、杀进程和连续 stable/final 回放；当前
  `emulator-5560` 属于其他任务，不能使用。
- 完整视频转写进入 Facts/Q2、长会人工事实支持率、行动有效性和引用相关性。
- capability barrier、旧任务排空、GPU 资源门、生产切换和 Stage 5 删除审计。
