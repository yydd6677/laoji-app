diff --git a/backend/app/services/app_summary_generator.py b/backend/app/services/app_summary_generator.py
--- a/backend/app/services/app_summary_generator.py
+++ b/backend/app/services/app_summary_generator.py
@@ -383,6 +383,9 @@
 def _compact_model_config():
     """Use the configured bounded-summary model."""
     config = _load_config()
+    summary_base_url = os.getenv("SUMMARY_OLLAMA_BASE_URL", "").strip()
+    if summary_base_url:
+        config = config._replace(base_url=summary_base_url.rstrip("/"))
     model = (
         os.getenv("APP_SUMMARY_COMPACT_MODEL", "qwen3.5:9b").strip()
         or "qwen3.5:9b"
diff --git a/backend/app/workers/summary_tasks.py b/backend/app/workers/summary_tasks.py
--- a/backend/app/workers/summary_tasks.py
+++ b/backend/app/workers/summary_tasks.py
@@ -39,6 +39,14 @@
     telemetry_scope,
 )
 
 
+def _summary_ollama_base_url(env: dict[str, str]) -> str:
+    return (
+        env.get("SUMMARY_OLLAMA_BASE_URL", "").strip()
+        or env.get("OLLAMA_BASE_URL", "").strip()
+        or "http://127.0.0.1:21434"
+    ).rstrip("/")
+
+
 def _configured_summary_worker_count(value: str | None = None) -> int:
     raw = os.getenv("SUMMARY_WORKER_CONCURRENCY", "1") if value is None else value
     try:
@@ -355,7 +363,7 @@
     env = os.environ.copy()
     workspace_root = meetingsummary_dir.parent.parent
-    env.setdefault("OLLAMA_BASE_URL", "http://127.0.0.1:21434")
+    env["OLLAMA_BASE_URL"] = _summary_ollama_base_url(env)
     env.setdefault("OLLAMA_HOST", "127.0.0.1:21434")
     env.setdefault("OLLAMA_MODELS", str(workspace_root / "ollama-models"))
     env.setdefault("OLLAMA_BIN", env.get("OLLAMA_BIN", "/home/zhong/.local/bin/ollama"))
